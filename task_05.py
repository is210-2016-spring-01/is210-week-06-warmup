#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""Task 05"""


def flip_keys(*to_flip):
    """1.  A list named ``to_flip``. This list is assumed to have nested,
        immutable sequences inside it, eg: ``[(1, 2, 3), 'hello']``"""
    #counter = 0
    for reverse in to_flip:
        reversed(to_flip)
        #to_flip.reverse()
        #tuple_reverse = to_flip[::-1] 
        #list_reverse.append(tuple_reverse)
        #to_flip = to_flip[::-1]
    #return counter
    #return tuple_reverse
        return to_flip
